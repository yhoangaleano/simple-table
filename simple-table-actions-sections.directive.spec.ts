import { SimpleTableActionsSectionsDirective } from './simple-table-actions-sections.directive';

describe('SimpleTableActionsSectionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleTableActionsSectionsDirective();
    expect(directive).toBeTruthy();
  });
});
